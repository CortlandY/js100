let a = 1;

function myFunction(a) {
  console.log(a);
}

let b = 2;

myFunction(b);

// 2; b is the argument passed into myFunction, and its value is 2.
// the variable a in line 4 references the argument passed to the function
